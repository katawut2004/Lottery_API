name: myapp
description: "แอพตรวจหวยไทย พร้อมฟีเจอร์สแกนและบันทึกประวัติ"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # UI และ State Management
  cupertino_icons: ^1.0.8
  get: ^4.7.2

  # API Request
  http: ^1.2.2

  # สแกนบาร์โค้ด / QR
  mobile_scanner: ^7.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
